{
  "name": "intelligent-feedback-app",
  "version": "1.0.0",
  "description": "Multi-Source AI Feedback Intelligence Platform",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
